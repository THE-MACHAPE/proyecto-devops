const request = require('supertest'); const app = require('./app.js'); describe('Pruebas', () => { it('Healthcheck', async () => { const res = await request(app).get('/api/health'); expect(res.statusCode).toEqual(200); expect(res.body.status).toBe('UP'); }); });
